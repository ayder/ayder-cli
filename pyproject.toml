[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ayder-cli"
version = "0.2.3"
description = "Interactive AI agent chat client for local LLMs"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "openai",
    "prompt-toolkit",
]

[project.scripts]
ayder = "ayder_cli.client:run_chat"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
]
